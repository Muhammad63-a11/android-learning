# Working directories created by the pipeline at run time.
/.doctor_workspace/
/output/

# Don't commit your Android project ZIP - it belongs in input/ as a
# local/CI-only file, not in git history (it can be large and is
# regenerated per build).
/input/*.zip
/input/*.ZIP
!/input/.gitkeep

__pycache__/
*.pyc
